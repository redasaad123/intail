on:
  schedule:
    - cron: "0 0 * * * "

jobs:
    daily-task:
      runs-on: ubuntu-latest
      steps:
        - name: print message
          run: echo "Running the scheduled task"
          
    
    
  
  
